.DS_Store
*.log

# personal Claude Code settings — not committed
.claude/settings.local.json

# local environment and secrets
.env
.env.*
!.env.example

# local Postgres data dir if ever created inside the repo, and scratch db url
/pgdata/

# local file storage and backup staging/repositories (spec 0001 T7, T14) —
# household data, never committed
/.data/
