-- The synthetic household (ADR 0015). Grows with each slice's tables; today
-- there is nothing to seed into beyond the bootstrap schema, so this is a
-- deliberate no-op that spec 0003's migration will give real content.
select 1; -- placeholder: real seed data arrives with the schema in spec 0003
