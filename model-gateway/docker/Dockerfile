# The model gateway stub — one file, no dependencies beyond the standard
# library, deliberately (spec 0001 T18).
FROM python:3.12-alpine
COPY server.py /server.py
EXPOSE 8090
CMD ["python3", "/server.py"]
